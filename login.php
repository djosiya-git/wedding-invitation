<?php require __DIR__.'/lib.php'; start_session(); if(!empty($_SESSION['admin'])){header('Location: index.php');exit;}
$error=''; if($_SERVER['REQUEST_METHOD']==='POST'){ if(hash_equals(cfg()['admin_username'],$_POST['username']??'') && hash_equals(cfg()['admin_password'],$_POST['password']??'')){$_SESSION['admin']=true;header('Location: index.php');exit;} $error='Username atau password salah.'; }
?><!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Login Admin</title><link rel="stylesheet" href="assets/admin.css"></head><body class="login-body"><main class="login-card"><div class="brand-mark">DW</div><h1>Invitation Admin</h1><p>Kelola template dan pesanan undangan dari satu dashboard.</p><?php if($error):?><div class="alert"><?=e($error)?></div><?php endif;?><form method="post"><label>Username<input name="username" required autofocus></label><label>Password<input type="password" name="password" required></label><button class="btn primary">Masuk Dashboard</button></form><small>Default: admin / admin123 — ganti di config.php</small></main></body></html>
